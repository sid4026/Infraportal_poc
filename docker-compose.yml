services:
  infra-portal:
    build: .
    container_name: acgi-infra-portal
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1